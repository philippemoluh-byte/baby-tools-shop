# Template env file: copy this to .env and set real values for your environment.
# Base settings (development)
BTS_SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Production overrides (uncomment and adjust)
# DEBUG=False
# ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# Database settings
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
